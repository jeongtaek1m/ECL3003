{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Python: DepthWithYolo (raw)",
            "type": "debugpy",
            "request": "launch",
            "program": "${workspaceFolder}/depth_with_yolo.py",
            "console": "integratedTerminal",
            "args": [
                "--raw"
            ],
            "cwd": "${workspaceFolder}"
        }
    ]
}
